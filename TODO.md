# TODO — Debito tecnico & azioni aperte

> Lista azionabile. Ogni voce: contesto, azione, file, criticità. Niente è bloccante per la build (vedi GO_NO_GO.md).

## 🔴 Alta priorità (attivazione produzione)
### ~~T1 — Attivare Nexus reale~~ → ✅ FATTO E VERIFICATO A SCALA REALE (2026-07-16)
- **Contesto:** provider HTTP implementato e disabled-safe; oggi gira il mock. **Fatto:** toggle in-app `nexusEnabled` (Impostazioni) + gating `main.ts` su env **oppure** setting; chiave cifrata già iniettata in provider + `download_link`.
- **Fatto (2026-07-09):** la chiave NON transita più in chiaro sull'IPC — `settings:get/get-all` restituiscono solo un segnaposto mascherato (`********`), gli handler `nexus:*` la leggono **esclusivamente** dal secret store del main (mai passata dal renderer). `nexus:validate-key` accetta un candidato appena digitato (non ancora salvato) senza mai esporre il valore persistito.
- **Azione residua storica (superata 2026-07-16):** ~~incollare la API key Premium ruotata in Impostazioni → Nexus, premere Verifica, attivare il toggle~~ — **fatto davvero dall'utente**, più volte nella sessione (l'ultima volta re-inserita dopo un problema), Premium **attivo**.
- **Verificato a scala reale (2026-07-16, PIVOT-07):** non solo `getMod`/`getFiles`/`checkUpdate` — l'utente ha importato e **installato interamente** la collection Nexus reale "Opoal Collection." (slug `frkafa`, revisione 159, ~1739 mod): import → coda download Premium (completata 1739/1739 dopo fix) → installazione → FOMOD applicato (235 mod) → conflitti risolti → deploy nella Data del gioco reale → preset ENB applicati → crash-watch armato. Questo chiude/supera drasticamente i residui storici "Nexus non testato a scala" presenti più sotto in questo file (E2E-DL/E2E-BATCH/Precheck-01/Run-Prog/Run-Real) — vedi nuova sezione "Pivot architetturale 2026-07-16" per il dettaglio completo.
- **File:** `electron/nexus/httpProvider.ts`, `electron/main.ts` (handler `nexus:*`), `src/components/pages/Settings.tsx`, `electron/nexus/collections.ts` (nuovo, flusso Collection).

### ~~Stock-UI — Pannello StockGame nel renderer~~ → ✅ FATTO
- `src/components/ui/StockGamePanel.tsx`: anteprima sorgente/destinazione + peso vanilla stimato/mod scartati (`stockGame.detect()`), bottone "Crea StockGame", barra di avanzamento live su `stockgame:progress`, scelta hardlink/copy. Cablato nella Dashboard.

### ~~E2E-DL — Test download reale end-to-end~~ → ✅ FATTO (2026-06-25)
- **Superato con Premium reale:** `4k Farmhouse Fences SE` (38912/153295, 50 MB) → link CDN (`cf-files.nexusmods.com`) → download → **md5 ✓** vs backup → `7z t` ✓ → **8 file estratti** (96,3 MB) in `data/StockGame/mods/`. Harness `scripts/e2e_download.mjs` riusabile.
- **Residuo prima della cancellazione di massa:** validare a **scala** (batch di 5–10 mod con formati misti .zip/.7z/.rar e un file grande >1 GB) prima di eliminare i 329 GB della cartella Vortex. La singola mod è provata; il batch no.
- **SUPERATO (2026-07-16, PIVOT-06/07):** il backup Vortex (`vortex-collections-backup.json`, i 4.568 mod/329 GB) è stato **eliminato deliberatamente** su richiesta esplicita dell'utente — non è più la fonte dati del progetto. La scala reale oggi provata è la collection Nexus "Opoal Collection." (slug `frkafa`, ~1739 mod) via il nuovo flusso Collection (`electron/nexus/collections.ts`), non più questo harness/dataset. Voce mantenuta per tracciabilità storica.

### ~~E2E-BATCH — Validazione a scala~~ → ✅ FATTO (2026-06-25)
- **4/4 superato:** `.7z`/`.zip`/`.rar` + file **1,01 GB**, concorrenza 3, **resume self-test** (caduta→Range→integro), tutti md5 ✓ + `7z t` ✓. Harness `scripts/e2e_batch.mjs`. Fix overflow `content-range` su >2 GB.
- **Verdetto cancellazione Vortex:** la pipeline è ora **blindata** (formati misti, file pesante, concorrenza, resume, integrità md5). Con il `vortex-collections-backup.json` come rete + tutto ri-scaricabile, **eliminare `…\Vortex\skyrimse\mods` è ragionevolmente sicuro**. Prudenza extra suggerita: prima del wipe totale, lanciare un batch un po' più ampio (es. 15–20 mod misti) per confidenza statistica.
- **SUPERATO (2026-07-16, PIVOT-06):** il wipe è avvenuto ed è andato oltre — la cartella Vortex **e la sua copia di sicurezza sono state entrambe eliminate** su richiesta esplicita dell'utente (2 volte). "La rete" descritta qui non esiste più; il dataset di riferimento oggi è la collection Nexus Opoal (~1739 mod), validata end-to-end su scala reale molto più ampia di questo batch di 4 mod. Voce mantenuta per tracciabilità storica.

### ~~Cabl-01 — Cablare il flusso reale nella Dashboard~~ → ✅ FATTO (2026-06-25)
- Orchestratore `electron/sync/massSync.ts` (resolve→download resumibile→md5 vs backup→estrai, concorrente, idempotente, cancellabile, **isolamento StockGame fail-closed**) + IPC `sync:start|cancel|status` + `window.api.sync`. Bottone "Sincronizza e Avvia" → conferma scale-aware + barra live `sync:progress`. 9 test.
- **Residuo (utente):** il run reale dei 329 GB parte solo cliccando il bottone nell'**app desktop** (non nel preview browser) con Nexus abilitato. Eventuale step finale: cablare anche la creazione StockGame come pre-fase del sync + lancio Pandora/launch post-sync.
- **SUPERATO (2026-07-16, PIVOT-06):** i "329 GB" citati qui erano il backup Vortex, oggi eliminato. Il flusso reale usato ed eseguito con successo è "Importa Collection Nexus" → download → installazione → deploy (vedi T1/PIVOT-07), non più questo `massSync` legato al dataset Vortex.

### ~~Precheck-01 — Pre-flight disco aggregato~~ → ✅ FATTO (2026-06-26; ricalcolato per blocco 2026-07-09)
- `massSync.ts`: `pendingBytes`/`computeDiskPreflight`/`diskPreflight` + enforcement bloccante fail-closed prima di ogni download; IPC `sync:preflight` + card GO/NO-GO Dashboard. +5 test.
- **Verdetto storico (2026-06-26): NO-GO** — 4.568 mod → richiesti **416,8 GB** (329,5 × 1.10 × 1.15) vs **276 GB liberi su C:** = margine **−140,8 GB**.
- **Aggiornamento (2026-07-09):** spazio libero su C: ricontrollato — ora **648,4 GB liberi** (282,1 GB usati), un salto di +372 GB rispetto al 26/06. Contro lo stesso requisito stimato (~416,8 GB per l'intera modlist), il margine tornerebbe **positivo (+231,6 GB)** → verdetto **presumibilmente GO** per l'intera lista, ma **da confermare nell'app** (la card Dashboard è la fonte di verità: il calcolo esatto dipende dal numero di mod ancora *pending* in quel momento). Con la card ora **per-blocco** (vedi Run-Prog sotto) il fabbisogno del primo blocco da 100 mod è comunque un ordine di grandezza più piccolo e quasi certamente GO anche nello scenario peggiore.
- **SUPERATO (2026-07-16, PIVOT-06/12):** i 4.568 mod erano il dataset Vortex, oggi inesistente. Il preflight NO-GO calcolato su quel numero non è più rilevante. Inoltre la politica spazio è cambiata nel frattempo (PIVOT-12): archivi eliminati di default dopo install riuscita — una pulizia one-shot reale in questa sessione ha liberato 73,8 GB (disco 94%→87%) più 23 GB in una pulizia precedente, quindi anche i margini di calcolo qui sopra sono superati dai fatti.

### ~~Run-Prog — Run reale progressivo~~ → ✅ IMPLEMENTATO (2026-07-09)
- **Fatto:** `sync:start` accetta `limit` e seleziona le **prossime N mod non ancora presenti** nello StockGame (progressione reale tra run successivi — non le prime N della lista, che dopo il primo run sarebbero tutte skip). `sync:preflight` valuta lo spazio del **solo blocco pianificato** quando `limit` è impostato, così la card GO/NO-GO riflette il run che sta per partire. Dashboard: campo "Blocco Run-Prog" (default **100**, 0 = intera lista), badge "blocco N/tot" sulla card pre-flight, conferma e log consapevoli del blocco.
- **File:** `electron/main.ts` (`selectSyncBlock`, handler `sync:start`/`sync:preflight`), `src/components/pages/Dashboard.tsx`, `electron/preload.ts`, `src/types/index.ts`.
- **Residuo (utente):** eseguire davvero la sequenza 100 → 300 → 500 → decisione finale, osservando throughput/ETA reali, comportamento di 429+breaker+resume al limite API (~2500 req/giorno) e cleanup a scala. Ogni click successivo su "Sincronizza e Avvia" riprende automaticamente dal punto giusto (nessuna azione manuale tra un blocco e l'altro).
- **SUPERATO (2026-07-16, PIVOT-06/07):** la sequenza 100→300→500 era pianificata sulla lista Vortex (4.568 mod), oggi non più esistente come dataset. La scala reale effettivamente eseguita ed osservata (throughput/ETA/429/resume su rete reale) è stata la collection Opoal (~1739 mod, completata 1739/1739) attraverso il flusso Collection, non attraverso `Run-Prog`/`massSync`.

### ~~Run-Real — Avvio grafico reale~~ → ✅ FATTO (2026-07-09)
- **Contesto:** tutto cablato; mancava solo l'esecuzione reale dall'app desktop.
- **Fatto:** build di produzione verificata (`tsc` + `vite build`, `dist-electron` pulita a ogni build) e **boot reale dell'app collaudato**: finestra avviata, migrazioni schema v6 applicate (inclusi gli indici), auto-detect percorsi riuscito, sorgente sync caricata (4.568 mod dal backup).
- **Avvio zero-sforzo:** `avvia_launcher.bat` in root — doppio click da Esplora Risorse: si posiziona da solo nel progetto (niente più ENOENT), ripara automaticamente un binario Electron mancante/corrotto, esegue `npm run build`, poi lancia `electron.exe` direttamente (bypassa del tutto la Execution Policy di PowerShell). Modalità rapida senza rebuild: `avvia_launcher.bat veloce`.
- **Azione utente:** doppio click su `avvia_launcher.bat` → Dashboard → verificare card pre-flight (GO) → (opz.) "Crea StockGame" → "Sincronizza e Avvia" per il primo blocco Run-Prog.
- **SUPERATO (2026-07-16):** "4.568 mod dal backup" qui sopra non è più un dato valido (backup Vortex eliminato, PIVOT-06). Il boot reale del gioco moddato oggi non passa più da questa pipeline `massSync`/StockGame come unica via: il lancio effettivo verificato è lo shortcut Desktop "Skyrim AE Fantasy Launcher" → SKSE interno (PIVOT-01), con deploy diretto anche sulla Data del gioco reale (PIVOT-02).

### ~~T2 — Manifest catalogo remoto firmato~~ → ✅ FATTO (2026-06-24)
- Catalogo reale firmato `electron/delta/examples/catalog.remote.signed.json` (release `2026.06-core`, counter 2, 6 mod con `version`/`file_id`/`file_hash` sha256 reale/host Nexus). Producer `scripts/build_remote_catalog.mjs` (parità Node del signer Python — stesso `canonicalJSON`+Ed25519, firma verificata dalla chiave pinnata). Cablato nella pagina Aggiornamenti. Test `electron/delta/remoteCatalog.test.ts` (4: verifica pinnata, schema, anti-tamper, ingest+drift).
- **Residuo:** è un artefatto **bundlato** (non ancora servito via HTTP da un host reale) e i `file_hash` sono sha256 di un descrittore deterministico finché non si dispone degli archivi reali. Il fetch su rete reale resta Act-03.

## 🏰 Pivot architetturale 2026-07-16 — launcher SKSE-only, deploy reale, engine LOOT in-house
> Sessione 2026-07-16: ha portato l'app da "companion-mode read-only intorno a MO2" a un launcher SKSE-only che scrive davvero nel gioco. **Tutti i documenti MD (questo TODO, ROADMAP, GO_NO_GO, RISK_MATRIX, TASKS) erano fermi al 2026-06-23/07-10 e descrivono su più punti un'architettura ormai superata** — questa sezione e le note di superamento sparse nel resto del file colmano il gap. Dettagli tecnici completi e wording di riferimento in `CHANGELOG.md` (sezioni FOMOD-01/INTEGRITY-01/QUEUE-01/DEPLOY-GAME-01/CRASH-01/SKSE-ONLY-01/LOOT-MASTERLIST-01/CATALOG-REBUILD-01/LOADORDER-01/etc), `MOD_CATALOG.md` (riscritto da zero) e `SESSION_STATE.md` (sezione 0 "Cambio di rotta architetturale").
>
> **Stack reale aggiornato:** Electron 43.1.0, better-sqlite3 12.11.1, electron-builder 26, Vite 8 (rolldown, no esbuild), Vitest 4, Node 24 — sostituisce ogni riferimento residuo a Electron 29.4.6/33 o better-sqlite3 9.6.0/11.3.0 che si trova più sotto in questo file (LAUNCH-01, D1, D1-storico): quei valori sono storici, non cancellati, segnalati come superati nelle rispettive sezioni.
>
> **Test suite reale aggiornata:** ~749 test / 76 file, tutti verdi; `tsc --noEmit` pulito; `vite build` OK — sostituisce ogni conteggio più vecchio nel file (95/220/266/447/etc) come stato **corrente** (i conteggi vecchi restano come traccia di progressione storica, non vanno cancellati).

### ~~PIVOT-01 — Avvio SKSE-only, MO2 fuori dal percorso di default~~ → ✅ FATTO
- MO2 rimosso dal percorso di avvio **di default**: `electron/launch/bootstrapper.ts` → `DEFAULT_BOOTSTRAPPERS = [skseBootstrapper, dragonLoaderBootstrapper]`; `mo2Bootstrapper` resta esportato ma inutilizzato. `preflight.ts` semplificato a solo SKSE. Avvio unico: shortcut Desktop "Skyrim AE Fantasy Launcher" → SKSE interno.
- **Nota:** ogni riferimento storico (in questo file, in ROADMAP/RISK_MATRIX) a "profilo MO2 attivo"/"parsing plugins.txt da MO2" come **dipendenza del load order** è superato dal PIVOT-03 sotto — MO2 non è più nel percorso del load order.

### ~~PIVOT-02 — Deploy scrive davvero nella Data del gioco reale~~ → ✅ FATTO
- `deployTarget: 'game'` scrive nella cartella Data del gioco reale (non più solo in uno StockGame isolato/virtualizzato). Reversibilità garantita: file pre-esistenti nel target salvati come `<file>.smm-vanilla.bak` prima di essere sovrascritti, il purge del manifest li ripristina; junction degradano a hardlink per-file con backup quando la dir di destinazione esiste già (caso normale su Data reale); euristica di pulizia disattivata sul target gioco reale (solo purge esatto da manifest).
- **Cambio filosofico:** il progetto non è più puramente "mai toccare il gioco reale" — oggi lo fa, ma in modo reversibile e tracciato (backup+manifest+purge), mai distruttivo silenzioso. Qualunque principio guida assoluto "mai modificare Steam/gioco" letto in ROADMAP/RISK_MATRIX va inteso corretto in questo senso. Vedi anche nota in ISO-01/ISO-02 più sotto.

### ~~PIVOT-03 — Load order: motore LOOT-like in-house (niente LOOT.exe, niente MO2)~~ → ✅ FATTO
- `electron/plugins/espParser.ts`: master reali letti dall'header binario TES4 di ogni plugin (verificato **355/355** su plugin reali). `electron/plugins/lootSort.ts`: topological sort (partizione master-space, blocco duro su master mancante/ciclo). `electron/plugins/lootMasterlist.ts` + `masterlistCache.ts`: scaricano e cachano la masterlist **community reale** da github `loot/skyrimse` (verificato: 3162 plugin, 47 gruppi, 429 regole, 872 dirty entries, parse 42ms) — fetch **solo su azione esplicita utente** (bottone "Aggiorna masterlist" in Strumenti), mai automatico. `electron/plugins/dirtyPluginCheck.ts` + `crc32.ts`: rilevano i plugin "dirty" (ITM/UDR/deleted nav) via CRC32 puro (verificato contro il test vector standard `0xCBF43926`) confrontato con le voci della masterlist. `electron/deploy/lootOrder.ts`: adapter che collega tutto al deploy, blocca (**prima** di scrivere) su ciclo di dipendenze, master mancante, o sforamento budget plugin.

### ~~PIVOT-04 — Budget plugin reale (254 full / 4096 light)~~ → ✅ FATTO
- Il deploy blocca (`errorKind: 'plugin-limit'`) **prima** di qualunque scrittura se si superano 254 slot "full" (ESM/ESP non-light: base+CC+mod) o 4096 slot "light" (ESL/FE), contati dai flag reali dell'header TES4 (non da una stima).

### ~~PIVOT-05 — Conflitti: nessuna disattivazione mod, resolution_weight + planner~~ → ✅ FATTO
- **Nessuna** disattivazione mod come meccanismo di risoluzione conflitti (mai). Colonna `resolution_weight` su `mods`/`modlist_catalog` (migrazione v8, già esistente da CAT-02) + planner del deploy (vince chi ha categoria > peso risoluzione > priorità/nome più alti). UI pagina Conflitti: sezione "Sovrascritture file (piano di deploy)" con dry-run reale (IPC `deploy:preview`) + bottone "Inverti precedenza" (IPC `deploy:prefer`, alza il `resolution_weight` della mod scelta).
- **Nota:** qualunque riferimento storico altrove in questo file (o in ROADMAP/RISK_MATRIX) a "disattivare una mod per risolvere un conflitto" è superato/corretto da questo pivot.

### ~~PIVOT-06 — Catalogo: niente auto-seed, wipe esplicito, import via Collection Nexus~~ → ✅ FATTO
- **Nessuna fonte automatica di popolamento** (mai più un auto-seed silenzioso — causa di una regressione ricorrente, corretta rimuovendo la `useEffect` colpevole in `Catalog.tsx`). Bottone "Svuota catalogo" (IPC `catalog:wipe`) cancella tutto e imposta il flag persistito `catalogSeedDisabled`, così nessun seed può ripopolare da solo. Via **primaria** di popolamento oggi: "Importa Collection Nexus" (`electron/nexus/collections.ts`) — `parseCollectionInput` accetta slug o URL (vecchio/nuovo formato/next.nexusmods.com), `fetchCollectionRevision` interroga il GraphQL v2 ufficiale di Nexus (`collectionRevision(slug, revision)`), scritto a mano perché il pacchetto npm pubblicato `@nexusmods/nexus-api` (tarball 1.1.5, verificato via `npm pack`) non contiene ancora questi metodi (presenti solo su GitHub master 1.7.1, non pubblicati).
- **L'import Vortex storico è STATO ELIMINATO DELIBERATAMENTE** (`data/vortex-collections-backup.json`, i **4.568 mod/329 GB** su cui erano basati moltissimi item di questo file — vedi note "SUPERATO" su E2E-DL/E2E-BATCH/Cabl-01/Precheck-01/Run-Prog/Run-Real più sopra) su richiesta esplicita dell'utente (2 volte, anche la copia di sicurezza). Quel bottone oggi fallisce con grazia ("Backup Vortex non trovato").

### ~~PIVOT-07 — Nexus reale attivo e provato a scala (1739 mod)~~ → ✅ FATTO — vedi anche T1 sopra
- Premium **attivo**, API key reale inserita e verificata in app (più volte nella sessione). Collection reale "Opoal Collection." (slug `frkafa`, revisione 159, Total Overhaul NSFW, ~1739 mod) **importata e installata interamente**: import → coda download (Premium, completata 1739/1739 dopo fix) → installazione → FOMOD applicato (235 mod, PIVOT-08) → conflitti risolti chirurgicamente (PIVOT-05) → deploy nella Data del gioco reale (PIVOT-02) → preset ENB applicati (PIVOT-09) → crash-watch armato (PIVOT-10).
- **Chiude/supera** quasi ogni residuo storico su "Nexus non testato a scala", "download reale non provato", "apply delta e2e con rete reale non testato", "e2e batch limitato a 4 mod" — ora provato su 1739 mod reali, non più un campione piccolo.

### ~~PIVOT-08 — Motore FOMOD ufficiale Vortex integrato~~ → ✅ FATTO
- `@nexusmods/fomod-installer-native` (N-API 8 stabile — .NET Native AOT, prebuilds win32-x64, **non serve rebuild** tra versioni Electron/Node). `electron/fomod/fomodApply.ts` (`runFomodHeadless`, `applyFomodInstructions` con journal RENAME+rollback+marker idempotente `.smm-fomod-applied.json`, varianti non scelte eliminate), `collectionChoices.ts` (parse `collection.json` della revision per le scelte del **curatore** della collection — non sono nel GraphQL, vivono nell'archivio scaricabile via `downloadLink`), `engine.ts` (IPC `fomod:fetch-choices/scan/apply-all`). Card Strumenti → FOMOD (Scarica scelte → Applica a tutte). Build: `build.asarUnpack` esteso ai file nativi del modulo + `build.npmRebuild:false` (il pacchetto shippa prebuilds, node-gyp lo romperebbe).

### ~~PIVOT-09 — Gestione preset ENB reale~~ → ✅ FATTO
- `electron/enb/`: scan nelle mod estratte, apply **copia** (non hardlink) i file nella root del gioco + backup `.smm-enb-bak` + manifest `.smm-enb-manifest.json`, remove ripristina. Avviso se manca il core `d3d11.dll` (enbdev.com, non ridistribuibile — resta azione utente scaricarlo separatamente).

### ~~PIVOT-10 — Crash log analyzer reale + crash-watch~~ → ✅ FATTO
- `electron/launch/crashLogAnalyzer.ts` (verificato su log reale 835 righe) + `electron/launch/crashEngine.ts` (`armCrashWatch`: poll 30s per 3h dopo ogni lancio riuscito, timer `unref()`'d, evento `crash:detected` → toast col modulo colpevole). Analisi manuale disponibile anche in Strumenti.

### ~~PIVOT-11 — Fix affidabilità download (RAR mascherati da .7z) + verdetto integrità api-provenance~~ → ✅ FATTO
- `sniffArchiveKind` (magic byte reali: `Rar!`/7z/zip) sostituisce il dispatch per estensione — root cause di **12/14 download "falliti"**: erano RAR reali serviti da Nexus con estensione `.7z`, rifiutati dal 7za bundlato senza codec Rar. Bottone "Riprova falliti" + IPC `download:retry-failed`. Nuovo verdetto integrità `api-provenance` (accettato **solo** senza hash atteso E quando l'URL viene dal resolver API autenticato per la coppia modId/fileId esatta, mai per URL diretti) ha chiuso i restanti 2/14.

### ~~PIVOT-12 — Politica spazio disco: archivio eliminato dopo install~~ → ✅ FATTO
- `deleteArchiveAfterInstall` di default dopo install riuscita (override `keepArchives` per disattivare). Pulizia one-shot reale eseguita in sessione: **1737 archivi, 73,8 GB liberati** (disco 94%→87%), oltre a 23 GB liberati in una pulizia precedente della sessione.

### PIVOT-13 — Build/packaging: disableAsarIntegrity + npmRebuild:false — DA RIVERIFICARE
- `build.disableAsarIntegrity:true` aggiunto (Windows Smart App Control blocca gli eseguibili Electron ri-patchati/non firmati dopo il patching PE-resource dell'asar-integrity di electron-builder — tradeoff accettato: nessuna validazione runtime dell'integrità asar, ma l'exe resta quello firmato/reputato di Electron). `build.npmRebuild:false` aggiunto (necessario per il modulo nativo FOMOD, vedi PIVOT-08).
- **Impatto sulla documentazione:** questi due cambi **invalidano potenzialmente** lo stato "GO_NO_GO packaging già verificato" scritto nei documenti vecchi (D1 sotto), che si riferiva a una build precedente (Electron 29/33). Va segnalato come **da riverificare con un build NSIS fresco su questo stack** — non è dato per assodato se un installer fresco con queste due opzioni sia già stato prodotto in questa sessione. Vedi D1/D1-storico più sotto.

### ~~PIVOT-14 — Backlog ricerca GitHub round 2 (focus FOMOD) — mai implementato~~ → voci aperte, vedi "Backlog ricerca FOMOD round 2" in fondo al file
- Emerso da una ricerca GitHub esplicita in questa sessione, non ancora implementato: preflight DLL SKSE, validazione header ESP, "Save Doctor", rilevamento external-changes sul deploy reale, INI tuner da BethINI Pie, grass cache autopilota, auto-clean QAC via SSEEdit. Dettaglio completo in fondo al file.

## 🔒 Sicurezza (hardening di produzione, 2026-07-09)
### ~~SEC-01 — Materiale crittografico fuori dal repo, sempre cifrato~~ → ✅ FATTO
- `scripts/sign_manifest.py`: la chiave privata di firma NON ha più un percorso fisso nel progetto — si risolve da `SKYRIM_RELEASE_PRIV_KEY_PATH` (o `--key`) e lo script **rifiuta** esplicitamente percorsi interni all'albero del repo. Su disco è **sempre** PKCS8 + `BestAvailableEncryption(passphrase)` (mai più `NoEncryption`); passphrase da prompt nascosto (`getpass`) o, in CI, da `SKYRIM_RELEASE_KEY_PASSPHRASE`. Nuovo comando `encrypt-key` per migrare una chiave storica in chiaro.
- `scripts/build_remote_catalog.mjs` (il secondo produttore, prima hardcodava `secrets/release_priv.pem`) allineato alla stessa risoluzione via env + supporto PEM cifrata.
- `scripts/protect_release_key.mjs` (nuovo, per macchine senza Python): migrazione one-shot — cifra, sposta fuori dal repo, e **valida il roundtrip di firma contro la chiave pubblica pinnata** prima di suggerire la cancellazione dell'originale. Eseguito con successo sulla chiave reale del progetto.
- **Azione residua (utente):** eseguire i 3 comandi di migrazione (`node scripts/protect_release_key.mjs` → `icacls` → `setx SKYRIM_RELEASE_PRIV_KEY_PATH`), poi cancellare `secrets/release_priv.pem` e `secrets/release_pub.pem` (la pubblica canonica è tracciata in `docs/keys/release_pub.pem`).

### ~~SEC-02 — `nexus.key` dismessa~~ → ✅ FATTO (codice) / 🔴 AZIONE UTENTE ANCORA APERTA
- Nessun codice applicativo l'ha mai letta (l'app usa il secret store cifrato via `safeStorage`/DPAPI). Gli script di sviluppo (`exec_boot.mjs`, `sync_smoke.ts`, `sync_batch_smoke.ts`, `e2e_download.mjs`, `e2e_batch.mjs`) ora richiedono **solo** `$env:NEXUS_API_KEY`.
- **Verificato 2026-07-10 (audit multi-sessione + riscontro diretto su disco):** `secrets/nexus.key` **esiste ancora**, 88 byte, in chiaro. Non è un residuo storico — è stato riconfermato presente da due sessioni indipendenti in date diverse (2026-07-09 e 2026-07-10) e appena ricontrollato ora.
- **Azione residua (utente, invariata):** cancellare `secrets/nexus.key` e **ruotare la chiave** su nexusmods.com (è rimasta in chiaro su disco → va considerata esposta). Sicuro cancellarla: l'app non la legge da tempo.

### ~~SEC-03 — API key mai esposta al renderer~~ → ✅ FATTO
- `settings:get`/`settings:get-all` restituiscono solo il segnaposto `********`; gli handler `nexus:search`/`nexus:get-mod` leggono la chiave **esclusivamente** main-side. `encryptSecret` ora è fail-closed (rifiuta di salvare se `safeStorage` non è disponibile, invece di ripiegare silenziosamente sul plaintext).

### ~~SEC-04 — Lockdown IPC (esecuzione arbitraria, canali, path traversal)~~ → ✅ FATTO
- `tools:launch-*` non accettano più un percorso `.exe` dal renderer: il main risolve l'eseguibile dal settings store (`toolPath()`); il renderer sceglie solo *quale* tool avviare. `electron/preload.ts`: whitelist esplicita dei 10 canali evento legittimi per `on()`/`off()` (prima si poteva sottoscrivere qualunque canale IPC interno); `off()` ora funziona anche passando il listener originale (non solo il wrapper). `fs:open-external` accetta solo `http(s)://`. `fs:read-dir` reso asincrono (niente più blocco del main process su directory grandi).

### ~~SEC-05 — `.gitignore` blindato + primo repository Git~~ → ✅ FATTO
- Il progetto **non era un repository Git** (rischio di versionamento nullo, segnalato in RISK_MATRIX R9) — ora inizializzato (`git init -b main`) con **4 commit** puliti. `.gitignore` esclude `secrets/*` (tranne il README di bonifica), `*.pem` (tranne `docs/keys/release_pub.pem`), `*.key`, `.env*`, i database locali (`*.db*`, `*.sqlite*`), `node_modules/`, `dist*/`, `release/`, `data/` (30+ GB di cache/StockGame). Verificato con `git check-ignore` su ogni segreto **prima** del primo commit; audit dei file in staging confermato senza materiale sensibile.

## ⚙️ Qualità del codice (2026-07-09)
### ~~QUAL-01 — ESLint 9 + Prettier + CI~~ → ✅ FATTO
- `eslint.config.js` (flat config): recommended JS + TypeScript + React Hooks, baseline pragmatica (violazioni di solo stile → warning, non bloccanti). `.prettierrc.json` calibrato sullo stile esistente. Script npm: `lint`, `lint:fix`, `format`, `format:check`, `typecheck`. Durante la calibrazione il lint ha scovato **3 bug latenti reali** (un `require()` CommonJS fragile in `electron/steam/detect.ts`, caratteri BOM letterali invisibili nei regex di `electron/steam/mo2.ts`, un escape regex errato in `scripts/exec_boot.mjs`) — tutti corretti.
- `.github/workflows/ci.yml`: pipeline GitHub Actions su `windows-latest` (coerente con la piattaforma target: `better-sqlite3` nativo + 7-Zip bundled), tre step lint → typecheck → test (266 test vitest) a ogni push/PR.
- **Stato attuale (riverificato 2026-07-10):** 0 errori ESLint, **3 warning** (era 26 — ridotti nelle sessioni successive, vedi T12), `tsc --noEmit` pulito, **447/447 test verdi (50 file)** — il numero è salito da 266 attraverso il lavoro di 6+ sessioni parallele su questo repo (catalogo/installer v2, risoluzione conflitti, INI service, CC handler, launcher one-click, hardening sicurezza, atomic extraction). `.github/workflows/ci.yml`: confermato `windows-latest`, `node-version-file: .nvmrc` (→ Node 24), step lint→typecheck→test, `npm ci --ignore-scripts` (i test usano `node:sqlite`, non serve compilare `better-sqlite3` nativo in CI). Remote `origin` configurato (`github.com/guidonossardi001-hue/skyrim-mod-manager`); branch locale **7 commit avanti** rispetto a `origin/main` (non pushati).
- **Progressione ulteriore (2026-07-16):** 447 → **~749 test verdi (76 file)** attraverso la sessione SKSE-only/deploy-reale/FOMOD/ENB/LOOT-masterlist (vedi sezione "Pivot architetturale 2026-07-16"), `tsc --noEmit` e `vite build` ancora puliti.

## 📈 Performance & refactor funzionale/reattivo (2026-07-09)
### ~~PERF-01 — Indici SQLite + throttling scritture~~ → ✅ FATTO
- Migrazione **v6**: indici su `mods(profile_id)`, `mods(nexus_id)`, `downloads(profile_id)`, `downloads(status)`, `downloads(mod_id)`, `delta_changeset(download_id)` — prima ogni query calda (liste mod/download, resume, callback delta) faceva full scan. Persistenza del progresso download throttlata a 1 scrittura/secondo per download (prima: una scrittura WAL per ogni tick da 250ms, fino a ~32/s con 8 download concorrenti).

### ~~PERF-02 — Virtualizzazione completa + selettori Zustand shallow~~ → ✅ FATTO
- `ModList.tsx`: **tutte e tre le viste** ora virtualizzate con `react-virtuoso` (prima solo il ramo filtrato/ordinato lo era — la vista di default, riordinabile via drag&drop, montava ~70.000 nodi DOM alla scala target di ~4.500 mod). Righe `React.memo` con handler stabili condivisi via `useMemo`, lookup conflitti O(1) (era `Array.find` per riga per render).
- `Downloads.tsx`: `DownloadRow` memoizzata (un evento di progresso ora aggiorna solo la riga interessata, non l'intera pagina — prima ~16 re-render/s dell'intera lista con 4 download attivi).
- Selettori `useShallow` sui componenti sempre montati (`App`, `Sidebar`, `TitleBar`) — prima ogni `set()` dello store (incluse le righe di log) ri-renderizzava l'intero albero.

### ~~PERF-03 — Batch IPC, cancellazione stale, error handling~~ → ✅ FATTO
- `appStore.ts`: guard di staleness (token incrementale) su `loadMods`/`loadDownloads` — la risposta IPC di un profilo vecchio, se arriva dopo un cambio profilo rapido, viene ora scartata invece di sovrascrivere i dati nuovi. Check aggiornamenti Nexus con pool a concorrenza 4 + un solo `set()` finale (prima: loop seriale, 15-40 minuti a 4.500 mod + un re-render globale per mod). Import MO2 in un solo batch IPC/transazione (`mods:add-many`, nuovo handler) invece di un round-trip per riga.
- `App.tsx` bootstrap con `try/finally` (un errore di init non lascia più l'app bloccata sull'overlay "Inizializzazione…"); handler globali `uncaughtException`/`unhandledRejection` nel main; mock backend caricato **solo in DEV** (in produzione un preload rotto ora fallisce visibilmente invece di ripiegare silenziosamente su dati simulati).

### ~~PERF-04 — Robustezza pipeline download/installer~~ → ✅ FATTO
- `downloadStream.ts`: gestito il caso 416 (`.part` già completo dopo un crash pre-promozione — prima restava bloccato per sempre, essendo un 4xx non ritentabile). `downloadManager.ts`: cache-hit ora validato sulla dimensione attesa (prima si fidava di qualsiasi file omonimo con size>0); nomi archivio includono il `file_id` Nexus (niente più collisioni tra file diversi della stessa mod); il task viene registrato **prima** degli `await` di rete (fix race sul limite di concorrenza + pause/cancel "fantasma" durante la risoluzione del link). `installManager.ts`: il cleanup su errore non cancella più una installazione preesistente in caso di reinstallazione fallita.

## 🖥️ Launcher desktop One-Click Play (2026-07-10)
### ~~LAUNCH-01 — Da companion-mode a launcher-first attivo~~ → ✅ FATTO
- **Contesto:** prima solo report read-only (avvisa, non agisce). Ora: doppio-click icona dragon → schermata Fantasy Launcher unica → GIOCA → pipeline 11 stadi automatica → gioco moddato. Vanilla mai avviato a mano.
- **Fatto:** `electron/steam/steamControl.ts` (Steam ATTIVO: avvia se giù, attende init, verifica login via registry `ActiveUser`, mai bypassa); `electron/launch/bootstrapper.ts` (astrazione modulare sostituibile, registry MO2→SKSE→DragonLoader `steam://run/489830`); `electron/launch/activeLaunch.ts` `runActiveLaunch` (orchestratore puro, pipeline ordine spec, stream progress, stop su fail critico); `electron/launch/launcherUpdate.ts` (self-update opzionale via `createRequire`, no-op se assente); `electron/util/launcherConfig.ts` (smart-startup: autoLaunch, ultima config, no riconfigurazione a ogni avvio).
- **UI:** `src/components/ui/LauncherShell.tsx` (schermata fantasy full-screen, checklist stadi live stile Nolvus), store `launcherActive` (default true), App.tsx launcher-first, Sidebar bottone GIOCA per tornare dal manager.
- **Distribuzione:** `package.json` — dragon icon (`assets/dragon_launcher.ico`, 9 risoluzioni 256→16) su finestra+installer+shortcut, `electron-updater` dep, NSIS `createDesktopShortcut`+`createStartMenuShortcut`, **`perMachine:false`+`deleteAppDataOnUninstall:false`** (dati utente preservati agli aggiornamenti), `publish` GitHub (`guidonossardi001-hue/skyrim-mod-manager`).
- **Verificato:** 447 test verdi (+40 nuovi), typecheck/lint/build prod puliti, percorso felice provato nel preview browser (11/11 stadi verdi via SKSE, gate blocca correttamente su Skyrim assente, round-trip Launcher↔Gestione Mod).
- **Residuo (utente):** packaging reale dell'installer .exe non eseguito qui — serve `npm install` (bump a Electron 33 + rebuild ABI `better-sqlite3`) poi `npm run electron:build`. Vedi D1 sotto, ora aggiornato con l'icona/config definitive. **[AGGIORNATO 2026-07-16]:** lo stack è nel frattempo salito a Electron 43.1.0/better-sqlite3 12.11.1 (non più 33/11.3.0) — vedi sezione "Pivot architetturale 2026-07-16" e D1/D1-storico per il dettaglio e per cosa resta da riverificare (PIVOT-13).
- **Contesto storico (BOOT_GATE_01, dalla sessione fondativa 2026-06-21→07-08):** un audit read-only a fine sessione fondativa aveva trovato nessun avvio mai riuscito del gioco moddato — `data/StockGame` conteneva solo la cache mod (271, ~22GB) senza base vanilla assemblata né manifest di deploy, `plugins.txt` vuoto (0 plugin attivi) nella cartella di gioco reale. LAUNCH-01 (questa sezione) è nato esattamente per chiudere questo gap con una pipeline attiva a 11 stadi — ma la sua verifica **11/11 verde è stata eseguita solo nel preview browser con IO mockato**, non nell'app Electron reale con Skyrim vero installato. Il boot reale del gioco moddato end-to-end resta quindi **non ancora dimostrato**.
- **Icona dragon — provenienza (2026-07-10):** `assets/dragon_launcher.ico` scaricata da iconarchive.com (set "Dragon", Google Noto Emoji), licenza **Apache 2.0** — uso commerciale consentito, nessuna attribuzione obbligatoria. 203 KB, 9 risoluzioni (fino a 256×256). Verificata come `.ico` valido (magic bytes MS Windows icon resource), risolta correttamente da `resolveLauncherIcon` in `electron/launcher/launcherService.ts`.
- **Riverificato (2026-07-10):** `tsc --noEmit` pulito, **447/447 test verdi** (50 file), lint 0 errori/3 warning invariati. `node_modules` locale ancora fermo su Electron 29.4.6/better-sqlite3 9.6.0 — l'`npm install` per il bump resta azione utente (vedi D1).
- **Riverificato (2026-07-16):** stack reale ora Electron 43.1.0/better-sqlite3 12.11.1/electron-builder 26/Vite 8/Vitest 4/Node 24 (superano i valori 29.4.6/9.6.0 sopra, ormai storici). Test suite salita a **~749 test / 76 file**, tutti verdi; `tsc --noEmit` pulito; `vite build` OK. Vedi sezione "Pivot architetturale 2026-07-16" in cima al file.

## 🧩 Catalogo firmato → Installer v2 → Deploy virtualizzato (2026-07-10)
> Documentato ora (2026-07-10) perché mai tracciato in TODO prima d'ora, pur essendo codice reale e testato nel repo. Costruito su più turni da una sessione dedicata ("Skyrim mod catalog ingestion service").

### CAT-01 — Pipeline completa: catalogo firmato → risolutore dipendenze → installer a ricetta → deploy virtualizzato
- **Catalogo firmato:** `electron/catalog/{types,verify,validate,service,fetch,engine}.ts` — `CatalogService.ingest()` verifica SHA256/Ed25519/anti-downgrade (stesse primitive di `delta/manifest.ts`), validazione all-or-nothing, `replaceAll` atomico in transazione. `fetchSignedCatalog` con difese SSRF. IPC `catalog:update`.
- **Risolutore dipendenze:** `electron/catalog/dependencies.ts` — `resolveInstallPlan()` con DFS iterativo, ordinamento topologico, rilevazione cicli, risoluzione conflitti, un'unica query per il grafo. IPC `catalog:resolve-plan`.
- **Installer a ricetta:** `electron/install/recipe.ts` (pianificatore puro rimappatura file stile FOMOD, guardia anti path-traversal) + migrazione **v7** (tabella `mod_install_recipe`) + `electron/install/installer.ts` (`InstallerService.installMod`: staging → estrazione 7z selettiva via filtri `-ir!` → `planRecipe` → commit atomico `renameSync` → cleanup) + `electron/install/engine.ts` (`sweepStaging` al boot). `installManager.ts` refactored a delegare extraction/recipe/commit a `InstallerService`, mantenendo solo responsabilità coda/DB/eventi.
- **Deploy virtualizzato:** `electron/deploy/plan.ts` (mappa override pura + split junction/hardlink + costruttore `plugins.txt`) + `deployer.ts` (`deployInstance`: guardia cross-volume, cleanup solo-link sicuro, garanzia read-only su StockGame/modsRoot) + `deploy/engine.ts` (IPC `deploy:run`, evento `deploy:progress`).
- **UI:** `src/components/ui/DependencyResolver.tsx` (stati success/conflict/cycle/missing/db, azioni Analizza/Procedi) cablato in `Catalog.tsx` (checkbox multi-select, drawer laterale, `handleProceed` che accoda il piano in ordine).
- **Verificato dalla sessione (non ri-eseguito in questo audit):** progressione test 250→265→284→299→305→319→321, tutti `tsc`/lint puliti a ogni step; verifica live nel preview browser (3 mod selezionate → 4 mod nel piano con Address Library auto-aggiunta come dipendenza → 4 download accodati in ordine corretto, zero errori console).
- **Residuo esplicito lasciato dalla sessione:** nessuna vera *ricetta* d'installazione popolata nel catalogo firmato (l'infrastruttura esiste, non i dati); nessuna UI dedicata al Deploy (solo eventi `deploy:progress` cablati, mai un bottone/pagina che li consuma); nessun commit git fatto in quella sessione.

### CAT-02 — Risoluzione automatica conflitti file + servizio INI + Creation Club handler
- **Motore conflitti:** `electron/deploy/plan.ts` — union `DeployCategory` (patch/gameplay/texture/mesh/misc) + `resolutionWeight` su `DeployMod`; `computeDeployPlan` con `conflictWinner` a 3 regole (categoria > peso risoluzione > priorità/nome) → `DeployPlan.resolvedConflicts`. UI: banner neutro "Conflitti Auto-risolti" in `DependencyResolver.tsx`.
- **Wiring DB reale:** migrazione **v8** (`deploy_category`/`resolution_weight` su `mods` e `modlist_catalog`, guardata con `columnExists`) — `LATEST_SCHEMA_VERSION` confermato = 8 sul disco. `catalog/service.ts` persiste i campi all'ingest; `installManager.ts` (`markInstalled()`) li riporta dal catalogo alla riga mod per `nexus_id`; `deployer.ts` li legge realmente invece di dati mock.
- **Servizio INI:** `electron/ini/iniService.ts` — `applyIniSettings()` editor line-based che preserva la struttura (non parse-object-reserialize: il pacchetto npm `ini` elimina i commenti), scrittura atomica tmp+rename. `electron/ini/templates.ts` (preset base/ultra/performance/vr + `MOD_REQUIRED_OVERRIDES`). Cablato come fase 7 del deploy, non bloccante su fallimento.
- **Creation Club handler:** `electron/deploy/ccHandler.ts` — rilevatore puro read-only (riusa regex già presenti in `stockGame.ts`), raggruppa esm/esl/bsa CC in `CCPackage[]` ordinati per `Skyrim.ccc`. Cablato nel deploy: rilevati a step 1.5, hardlinkati a step 5.5 (salvo patch di una mod sullo stesso filename), iniettati in `plugins.txt` subito dopo i master DLC base.
- **Verificato dalla sessione:** progressione 369→376→390→400 test, `tsc`/eslint puliti a ogni fase.
- **File nuovi confermati su disco (2026-07-10):** `electron/deploy/ccHandler.ts`, `ccHandler.test.ts`, `electron/ini/iniService.ts`, `iniService.test.ts`, `templates.ts` — tutti presenti e verificati.

## 🧱 Isolamento StockGame + robustezza download/estrazione (2026-07-09/10)
> Anche questo mai tracciato in TODO fino ad ora, pur essendo codice reale confermato sul disco. Da due sessioni indipendenti (audit installer + "atomic extraction priority").
> **Nota (2026-07-16, PIVOT-02):** da questa sessione esiste **anche** un percorso di deploy diretto sul gioco reale (`deployTarget: 'game'`, backup+manifest+purge reversibile) accanto all'isolamento StockGame descritto qui sotto — le due sezioni ISO-01/ISO-02 non descrivono più l'**unica** modalità di deploy del progetto, solo quella verso lo StockGame isolato/virtualizzato. Vedi sezione "Pivot architetturale 2026-07-16" più sopra.

### ISO-01 — Isolamento ibrido StockGame + recupero da troncamento
- **Fatto:** `electron/install/stockGame.ts` — `requiresPhysicalCopy()` (verificato riga 106): copia fisica per file mutabili/critici (`SkyrimSE.exe`, `SkyrimSELauncher.exe`, plugin ESM/ESL/ccc), hardlink solo per asset bulk immutabili (BSA/Video/Strings). Prima i hardlink condividevano l'inode con i file Steam reali → una patch/xEdit in-place avrebbe corrotto l'installazione Steam vera; ora è impossibile per i file critici.
- **Fatto:** `placeFile`/`placeFileAsync` non si fidano più di `existsSync(dest)` come prova di "già installato" — un file troncato da crash/ENOSPC viene ora rilevato (confronto dimensione) e riparato, non trattato come completo. Posizionamento atomico via `${dest}.tmp` + rename (verificato righe 288/418).
- **Test:** describe "hybrid isolation policy" (classificazione `requiresPhysicalCopy` + conteggio `nlink`: copie fisiche nlink=1, hardlink bulk nlink≥2) + test recupero file troncato, in `stockGame.test.ts`.

### ISO-02 — Estrazione atomica confermata + preflight disco onesto + hardening download
- **Verificato (non nuovo, già presente da commit `03a9760`):** `extractArchive` estrae sempre in dir temporanea + rename finale — nessun archivio a metà può apparire come completo. 4 test di regressione aggiunti per bloccare la garanzia.
- **Fatto:** `computeDiskPreflight` (`massSync.ts`) — `extractionOverhead` da 1.10 a **1.5** (confermato sul disco, riga 92), cache archivi (`pending`) ora contata su **entrambi** i rami same-disk/cross-disk (prima il ramo cross-disk la ignorava, sottostimando lo spazio richiesto).
- **Fatto:** `classifyDownloadFailure` (confermato in `electron/install/retryPolicy.ts`, riga 74) — helper puro che classifica retry/abort/permanente sullo stato **autoritativo** `AbortSignal.aborted` invece che su substring del messaggio (bug reale trovato da una review avversariale: `'annullato'` veniva marcato erroneamente `'failed'`, e `'ECONNABORTED'` — che contiene la stringa `'abort'` — veniva scambiato per una cancellazione). `downloadLink.ts`: nuovo `DownloadLinkError` tipizzato che preserva `.status`/`.cause` per la classificazione retry. `downloadStream.ts`: watchdog stallo connessione (soglia 30s) → `StallError` ritentabile.
- **Verificato dalla sessione (non ri-eseguito qui):** progressione test 269→273→274→279→284, `tsc` pulito a ogni step, commit `e1eb268`+`0cd3eac`.

## 🛡️ Security Review Board — Ciclo 1 (2026-07-10)
### SEC-06 — Audit multi-agente + hardening reale (71 sub-agenti: OPUS lettori/sintesi, SONNET fix, HAIKU verifica avversariale)
- **Contesto:** sessione dedicata ha eseguito un Workflow con 7 lettori OPUS + 12 audit-dimensione SONNET + verifica HAIKU per-finding contro il codice reale. 51 finding grezzi → **37 confermati** dopo verifica avversariale (14 respinti). HAIKU ha anche respinto alcune fix di primo tentativo di SONNET perché causavano regressioni (es. un rifiuto ingenuo di chiavi path-like in `settings:set` avrebbe rotto il salvataggio Impostazioni) — applicate fix corrette a livello di sink.
- **Fatto (~14-15 fix, verificate presenti sul disco 2026-07-10):** `electron/main.ts` (guardia chiave piatta su `settings:set`, allow-list basename su `toolPath()`, gate di identità pre-spawn su `tools:validate-7z`, `downloads:list` con colonne esplicite per non far trapelare `nxm_key`/`nxm_expires` al renderer, CSP rinforzata con `object-src`/`base-uri`/`form-action`/`frame-ancestors 'none'`, **`sandbox: true`** in `webPreferences` + permission handler deny-all, validazione intera su `nexusId`); `electron/util/paths.ts` (`sanitizePathSegment` ora rifiuta `.`/`..`/nomi dispositivo riservati Windows/punti-spazi finali — **l'unico finding CONFERMATO sfruttabile**, con 7 test di regressione in `paths.test.ts`); `electron/install/sevenZip.ts` (allow-list basename `^(7z|7za|7zg|7zr)(\.exe)?$` su `detect7zPath`); `electron/downloadManager.ts` (`assertSafeDirectUrl`: solo https + blocco host interni/loopback); `electron/steam/detect.ts` + `electron/launcher/launcherService.ts` (`reg.exe`/`tasklist.exe`/`wscript.exe` ora invocati per path assoluto System32, non più per nome nudo — chiude un vettore di binary-planting via PATH/CWD); `electron/install/extract.ts` (fallback in-memory adm-zip ora limita anche la dimensione **non compressa**, non solo quella compressa — chiude un OOM da decompression-bomb); `electron/logger.ts` (rimozione caratteri di controllo + cap lunghezza riga su nomi non fidati, contro log-injection); `electron/delta/pinnedKey.ts` (override env `NOLVUS_MANIFEST_PUBKEY` ora gated a `NODE_ENV` dev/test/ci, la chiave Ed25519 pinnata non è più sostituibile in produzione).
- **Report:** `docs/security/SECURITY-REVIEW-{OPUS,SONNET,HAIKU}.md` (confermati presenti sul disco).
- **Verificato indipendentemente 2026-07-10:** tutte le fix sopra sono realmente presenti nel codice sorgente (non solo dichiarate) — confermato via lettura diretta dei file.
- **Residuo — backlog P1/P2 esplicito, NON in questo ciclo:** controllo freschezza/rollback update (`published_at` non verificato), hash-gating obbligatorio sui download non-delta, confinamento a root noti per `fs:open-path`/`fs:read-dir`, gate di consenso sul protocollo `nxm://`, cap di concorrenza sulle installazioni, firma del codice dell'installer, drift lockfile↔manifest, comportamento fail-open di `sign_manifest.py`.
- **Azione utente:** la sessione invita esplicitamente a lanciare un "Ciclo 2" della Security Board per chiudere gli item P1 (rollback/hash-gating) — non ancora avviato. Raccomanda anche uno smoke-test manuale del lancio app (finestra si apre, download/installazione funzionano, nessun break IPC/console) prima del rilascio, perché `sandbox:true`/permission handler/CSP sono comportamenti runtime Electron non esercitati da Vitest.

## 🚀 Produzione (2026-07-09)
### ~~PROD-01 — Icona placeholder per il packaging~~ → ✅ FATTO
- `resources/icons/icon.ico` era referenziata (`package.json` build config, `electron/main.ts`) ma **inesistente** — `electron-builder` sarebbe fallito al primo build dell'installer. Generata via `scripts/make_placeholder_icon.mjs` (zero dipendenze, PNG 256×256 + container ICO scritti a mano). Rigenerabile con `npm run icon:placeholder`; da sostituire con l'icona definitiva quando disponibile (stesso percorso).

### ~~PROD-02 — Igiene build/packaging~~ → ✅ FATTO
- `vite.config.ts`: `emptyOutDir: true` sul build del main process (prima `dist-electron/` accumulava ~50 bundle hashati stale tra una build e l'altra, tutti spediti nell'installer). `package.json` build.files: rimosso `node_modules/**/*` ridondante (electron-builder colleziona già le sole dipendenze di produzione). `electron/logger.ts`: rotazione del log a 5 MB (prima cresceva senza limite). Corretto un bug reale: il pulsante Pandora in `Tools.tsx` chiamava `launchMO2` invece di `launchPandora`.

## 🟠 Media priorità (debito tecnico)
### ~~T3 — Parsing reale plugins.txt~~ → ✅ FATTO (2026-06-23)
- `electron/steam/mo2.ts`: risoluzione **portable + instance-mode** (`%LOCALAPPDATA%/ModOrganizer/<istanza>`), `selected_profile`/`@ByteArray`, override `profiles_directory` (assoluto/relativo/`%BASE_DIR%`), fallback per mtime del `plugins.txt`, guard path-traversal, letture BOM-tolerant. Cablato in `buildLaunchEnv` → `VerifyLoadOrder` valuta il vero plugins.txt. Review avversariale MO2 applicata. Test: `electron/steam/mo2.test.ts` (11).

### ~~T4 — backupManager usa snapshot.ts~~ → ✅ FATTO (2026-06-23)
- `electron/backup/manager.ts` (core electron-free): atomic write + checksum + `backup:auto` cattura whole-DB snapshot; restore rifiuta i corrotti; lockstep colonne delta provato. `backupManager.ts` ora è un thin IPC layer. Test: `electron/backup/manager.test.ts` (5).

### ~~T5 — Versione runtime Skyrim & compatibilità SKSE~~ → ✅ FATTO (2026-06-23)
- `electron/steam/version.ts` (parse `version-1-6-1170-0.bin` + `skse64_1_6_1170.dll`, match su 3 componenti, `null`=no blocco spurio) + `detectSkse()` in `detect.ts` (multi-bin → versione più alta) cablato in `preflight` → `gameVersionSupported` popolato realmente. Test: `electron/steam/version.test.ts` (8). NB: reviewer T5 caduto per limite sessione → auto-review applicata sui casi noti (multi-bin, VR/loader esclusi dal regex, fail-safe null).

## 🟢 Fatto in sessioni precedenti
### ~~Foundation Nolvus Ascension nel catalogo ("il necessario")~~ → ✅ FATTO (2026-06-25)
- +12 framework/SKSE-plugin base dalla guida ufficiale Nolvus Ascension (ID Nexus reali verificati). Sono le dipendenze richieste dalle mod di contenuto (script libs/distributori/hook). Esclusi i grafici (ENB Helper/Upscaler) come da vincolo. Catalogo 115→127 voci; peso invariato (foundation = pochi MB).
- **Riconferma:** il peso non sta nella foundation né nei contenuti non-grafici; i 300 GB sono texture (escluse).

### ~~Catalogo esteso: mod non-grafiche reali + ID corretti~~ → ✅ FATTO (2026-06-25)
- +12 mod reali verificate su nexusmods.com (quest/nuove-terre/audio/combat/survival), 103→115 voci, +10.8 GB (35.4 GB tot). Corretti 6 ID Nexus errati (link download reali). ID verificati via WebSearch (memoria spesso sbagliata).
- **Limite onesto (NON aggirabile):** i 300 GB sono ~tutti texture/grafica, categoria esclusa. I contenuti non-grafici restano nell'ordine delle decine di GB. Per altre voci serve verificare altri ID reali (posso continuare su richiesta).
- **Residuo pre-esistente (non introdotto ora):** 3 `nexus_id` duplicati nel catalogo originale [1137, 45720, 18975] — da bonificare separatamente.

### ~~Dashboard "Sincronizza e Avvia" (counter, gauge 300 GB, console, opt-in)~~ → ✅ FATTO (2026-06-25)
- `Dashboard.tsx`: hero panel con counter animato mod uniche + `DiskGauge` SVG (occupato/300 GB) + bottone centrale Nolvus-style (pipeline solo dopo clic) + toggle opt-in zero-clic con `window.confirm`; `LogConsole` scorrevole in fondo (auto-scroll, color-coded). Store: `activityLog`/`pushLog`/`clearLog`/`vortexStats`/`loadVortexStats`; `settings.autoSyncOnLaunch`; scan `totalBytes`. Verificato nel preview (833 / 288 GB / 13 righe log).
- **Nota:** l'opt-in zero-clic è l'unica via per l'avvio automatico della pipeline distruttiva, e richiede conferma esplicita all'attivazione (consenso una-tantum) — coerente con la safety approvata.

### ~~Importer Vortex + automazione gated all'avvio~~ → ✅ FATTO (2026-06-24)
- `electron/vortex/scan.ts`: scan read-only `collection.json` (modId/fileId/md5/size/optional) + nomi cartella, de-dup, build catalog (flag risorse base). Scan auto all'avvio (read-only), IPC `vortex:scan`/`build-catalog`, Pandora one-click gated (`tools:launch-pandora`). UI Strumenti. Test `scan.test.ts` (9). Validato sui dati reali (833 uniche / 2 collezioni).
- **Deviazioni motivate:** (1) richiesto in **C#** → fatto in **TS** (C# = codice morto nello stack Electron). (2) `__vortex_meta.json` **non esiste** in questa install → fonte reale = `collection.json` + nomi cartella. (3) **niente** download+estrazione+Pandora silenziosi a ogni avvio (distruttivo/irreversibile) → pipeline distruttiva dietro consenso one-click; solo lo scan è automatico.
- **Residuo:** il "one-click full pipeline" attuale fa scan→catalog→(Pandora gated). Il download di massa delle 833 mod richiede Nexus Premium e le mod sono già in Vortex; l'apply reale end-to-end è coperto da Arc-01/Act-03 ma non esercitato su questo set senza credenziali.

### ~~Sezione "Licenze di terze parti" (conformità LGPL/unRAR)~~ → ✅ FATTO (2026-06-24)
- `src/data/licenses.ts` (licenza 7-Zip integrale come stringa-asset + elenco componenti); UI Impostazioni con chip + scroll view (`<pre>` max-h/overflow); licenza anche in `resources/7zip-full/7-Zip-License.txt`. Test `licenses.test.ts` (guardia conformità). Verificato scrollabile nel preview.
- **Nota:** testo allineato alla versione 7-Zip **26.01** effettivamente bundlata (il file fornito riportava 1999-2021, termini identici; differiva solo l'anno di copyright).

### ~~.rar nativo: 7-Zip sistema primario + full 7z bundlato fallback~~ → ✅ FATTO (2026-06-24)
- `resolveRar7z()`: `detect7zPath` (sistema) primario → full 7z bundlato (`resources/7zip-full/`, asar-unpacked via `extraResources`, Rar/Rar5 verificato) fallback → notifica. `extract.ts` `full7zPath` vs `bundled7zaPath`. Settings: override opzionale. Test resolver + codec Rar reale.
- **Residuo cross-platform:** il full 7z bundlato è il binario **Windows**; su mac/linux i `.rar` ricadono su `detect7zPath` (p7zip-full di sistema) o notifica. Coerente col focus Windows del progetto. Niente test di estrazione `.rar` reale (manca un encoder rar per creare il fixture) → verificata la presenza del codec nel binario.

### ~~Estrazione nativa .7z/.zip senza configurazione~~ → ✅ FATTO (2026-06-24)
- `7zip-bin` bundlato (prebuilt, **niente C++** — rispetta il vincolo di progetto) → `.7z`/`.zip` multi-GB estratti out-of-the-box in **child process** (off-UI) con progress `-bsp1`. `extract.ts` format-aware; `.rar` via 7-Zip completo di sistema (lo standalone 7za non ha il codec Rar, verificato). Settings: 7-Zip opzionale. Test `extractNative.test.ts` (e2e reale) + `sevenZip.test.ts`.
- **Residuo `.rar`:** richiede ancora il 7-Zip **completo** installato (auto-rilevato). Alternativa non adottata: unrar WASM → carica in RAM, inadatto al multi-GB. Da valutare solo se i `.rar` diventano frequenti.

### ~~Reverse-eng. API Nolvus + pre-flight spazio disco~~ → ✅ FATTO (2026-06-24)
- Analizzato `Vcc.Nolvus.Api.Installer.dll` → `docs/NOLVUS-API-REFERENCE.md` (API proprietaria auth-gated → **non integrata**). Applicato `electron/install/diskSpace.ts` (stima ingombro estrazione + `fs.statfs`, fail-open) cablato in `installManager` prima dell'unpack. Test `diskSpace.test.ts` (8).
- **Spunto futuro (non fatto):** pre-flight requisiti hardware (GPU/VRAM/RAM vs soglie modlist) come estensione di Compatibilità — richiede dati requisiti nel catalogo (oggi assenti).

### ~~Protocol handler nxm:// (Premium + non-Premium)~~ → ✅ FATTO (2026-06-24)
- `electron/nexus/nxm.ts` (parse/find/createNxmDownload) + migrazione **v5** (`downloads.nxm_key/nxm_expires`); `main.ts` `setAsDefaultProtocolClient('nxm')` + single-instance rigoroso (seconda istanza → argv → primaria accoda) + cold-start + `open-url` (macOS) + `protocols` in electron-builder. `resolveUrl` inoltra key/expires; renderer naviga su `nxm:queued`. Test `nxm.test.ts` (8).
- **Residuo:** registrazione di sistema effettiva avviene a runtime (HKCU) o all'install (NSIS `protocols`); il flusso completo va provato con un vero click nxm dal browser + account Nexus reale. Il flusso Premium (download diretto) e non-Premium (key/expires) sono entrambi cablati ma non esercitati end-to-end senza credenziali reali.

### ~~Chiave API Nexus persistita nel DB (cifrata)~~ → ✅ FATTO (2026-06-24; irrobustito 2026-07-09, vedi SEC-03)
- Migrazione **v4** + `electron/db/secrets.ts` (tabella `app_secrets`, valore cifrato DPAPI via `safeStorage`); `settings:get/set/get-all` instradano i secret al DB con migrazione automatica dal vecchio `electron-store`. `downloadManager.getApiKey`/provider Nexus leggono dal DB → header reale `apikey`/`Bearer`. Test `secrets.test.ts` (6), smoke→v4.
- **Nota sicurezza:** il valore in `app_secrets` è SEMPRE ciphertext (mai chiaro), coerente con la regola "mai chiave in chiaro". Il file DB è in `userData` (non nel repo).

### ~~Nexus download_link + validazione 7-Zip~~ → ✅ FATTO (2026-06-24)
- `electron/nexus/downloadLink.ts`: endpoint reale con header `apikey`/`Authorization: Bearer` + nxm `key`/`expires`, errori parlanti (401/403→Premium, 404, 429); cablato in `downloadManager.resolveUrl`. `electron/install/sevenZip.ts` + IPC `tools:validate-7z` (auto-detect + validazione binario) + sezione Impostazioni (Sfoglia/Rileva/Verifica + warning). Test: `downloadLink.test.ts` (10) + `sevenZip.test.ts` (5).
- **Residuo:** download diretto reale = Nexus **Premium** (o flusso nxm:// per non-premium, non ancora gestito a livello di protocol handler). OAuth Bearer è supportato nell'header ma non c'è ancora un flusso di login OAuth.

### ~~Archivi reali — download streaming + estrazione sicura~~ → ✅ FATTO (2026-06-24; irrobustito 2026-07-09, vedi PERF-04)
- `electron/install/downloadStream.ts` (`.part` + resume Range, promozione atomica, integrità) e `electron/install/extract.ts` (7z streaming + progress, zip-slip guard, OOM cap adm-zip, sha256 streaming) cablati in `downloadManager`/`installManager` (verifica hash pre-estrazione, cleanup su errore). UI: stadio installazione (verifica/estrazione %) via `install:progress`. Test: `extract.test.ts` + `downloadStream.test.ts` (13, +2 per il caso 416).
- **Residuo:** il download REALE end-to-end richiede una chiave Nexus Premium (link `download_link.json`) non disponibile in questo ambiente → provato l'intero stack (resume/integrità/estrazione/hash) con server HTTP e archivi reali in test, ma non un vero scaricamento multi-GB da Nexus. L'estrazione `.7z`/`.rar` richiede `7z.exe` configurato (path in Impostazioni).

### ~~Act-03 — Fetch HTTP reale del catalogo + verifica firma~~ → ✅ FATTO (2026-06-24)
- `electron/delta/fetchCatalog.ts` (SSRF-safe: `new URL`+host allow-list, solo HTTPS, no-redirect, size cap, timeout) → IPC `delta:ingest-url` (fetch → trust boundary `ingest`). Campo Impostazioni `catalogUrl`; pagina Aggiornamenti usa fetch HTTPS se impostato. Test e2e con **server HTTP reale** `electron/delta/fetchCatalog.test.ts` (7: socket reale + verify + ingest; host/protocollo/oversize/redirect rifiutati; tamper→firma blocca).
- **Residuo:** in produzione serve un host reale che pubblichi `catalog.remote.signed.json` su HTTPS (oggi `NOLVUS_CATALOG_HOSTS` default = GitHub). Il download e l'apply degli **archivi** multi-GB su rete reale resta da provare end-to-end con file veri.

### ~~Delta non applicati~~ → ✅ FATTO (2026-06-24)
- Causa: `installed_snapshot` scritto solo da `finalizeApply` → nessun baseline per modlist installate normalmente. Soluzione: `electron/delta/snapshot.ts` (`syncInstalledSnapshot`) semina/riconcilia il baseline dalle mod installate (preserva `release_id`); `DeltaService.checkUpdates` diffa baseline vs ultimo manifest firmato; `store.checkAllUpdates` cablato al motore (fallback Nexus solo senza release). IPC `delta:sync-snapshot`/`delta:check-updates`. Test: `electron/delta/snapshot.test.ts` (6).
- **Residuo:** il manifest reale resta quello d'esempio finché non si pubblica il catalogo remoto firmato (T2). Mod senza `nexus_id` (alcuni import Wabbajack) non sono tracciabili nello snapshot per design.

### ~~Blocco 6 — Esporre i motori nella UI~~ → ✅ FATTO (2026-06-23)
- **Aggiornamenti** (`Updates.tsx`) sul motore delta (`window.api.delta.*`): stepper firma→delta→download→commit, changeset, apply+finalize gated.
- **Compatibilità** (`Compatibility.tsx`) su nuovo IPC `compat:analyze` (`electron/launch/compat.ts`): runtime Skyrim/SKSE (T5) + report `plugins.txt` MO2 (T3), classificazione ESM/ESP/ESL, budget 254, version drift.
- Mock browser `delta`+`compat` allineato all'IPC reale; `derivePluginsFromMods` estratta in `src/lib/plugins.ts`. Verificato nel preview; `tsc`/`vite build` OK, 116 test verdi.
- **Residuo:** la pagina Aggiornamenti in Electron ingerisce il manifest firmato d'esempio (bundle); l'aggancio al catalogo remoto reale resta T2.

## 🔵 Bassa priorità (miglioramenti)
- ~~**T6**~~ → ✅ **SUPERATO (verificato 2026-07-17)**: `catalog:list` (main.ts:2314) già supporta `search` su nome/descrizione/autore (LIKE); Catalog.tsx ha la barra di ricerca wired (`src/components/pages/Catalog.tsx:45,652`). L'API pubblica Nexus non serve: la ricerca opera sul catalogo locale, non su Nexus.
- ~~**T7** — Hash pre-estrazione cablato nell'install pipeline~~ → ✅ FATTO (2026-06-24): `electron/install/extract.ts` (`verifyArchiveHash` sha256 streaming) cablato in `installManager` da `delta_changeset.to_file_hash`. **NB:** i `file_hash` del catalogo d'esempio sono sintetici → un download REALE (chiave Nexus) verrebbe rifiutato finché non si pubblica un catalogo con hash di archivi veri (atteso, corretto: meglio fail-closed che estrarre non verificato).
- ~~**T8** — Endpoint `nexus:search` legacy~~ → ✅ **SUPERATO (verificato 2026-07-17)**: nessuna occorrenza nel codice (main.ts/preload/types/mock), già rimosso in una sessione precedente non documentata qui.
- **T9** — Coverage: alzare verso 95%+ includendo i wrapper electron (oggi testato il core puro/DB; le sonde sono machine-dependent).
- ~~**T10** — `launchGame` rimosso~~ → ✅ **OBSOLETO/SUPERATO (2026-07-10)**: `launchGame` esiste ed è attivo (`electron/launcher/launcherService.ts`, spawn detached + `stdio:'ignore'`, cablato in `main.ts`) — nota originale non più valida. Verificato anche `toast` in `Dashboard.tsx`: importato e usato (4 chiamate), non morto.
- ~~**T11** — Backup incrementali + compressione~~ → ✅ **PARZIALE, valore reale (2026-07-17)**: compressione gzip implementata (`electron/backup/manager.ts`, nuovi backup `.json.gz`, ~80%+ più piccoli; retrocompatibile — `listBackups`/`restoreProfileBackup` riconoscono ancora i vecchi `.json` in chiaro via sniffing dei magic byte gzip, zero migrazione richiesta). Incrementale **scartato deliberatamente**: il contenuto del backup è un dump JSON di righe DB (profilo+mod), non file — su questo volume di dati un diff incrementale sarebbe complessità senza beneficio reale; la compressione è il guadagno che conta davvero qui. Snapshot `.db` (VACUUM INTO, raro/pre-delta) lasciato non compresso apposta: resta un artefatto di recovery manuale copiabile senza decomprimere. +7 test.
- ~~**T12** — 26 warning ESLint residui~~ → ridotti a **3** (verificato 2026-07-10 su più sessioni parallele + riverifica diretta): `Catalog.tsx:63`, `Dashboard.tsx:275`, `Toast.tsx:32`, tutti `react-hooks/exhaustive-deps` su dipendenze intenzionalmente omesse (documentate inline). 0 errori.
- ~~**T13**~~ → ✅ **FATTO (verificato 2026-07-17)**: `secrets/` contiene solo `README-SICUREZZA.md` — sia `release_pub.pem` che `release_priv.pem` sono assenti dal filesystem (rimossi in una sessione precedente). Canonica resta `docs/keys/release_pub.pem`. SEC-01 chiuso al 100%.

## 🔬 Backlog ricerca FOMOD round 2 (2026-07-16) → chiuso al 100% (2026-07-18)
> Emerso da una ricerca GitHub esplicita nella sessione 2026-07-16 (round 2, focus FOMOD). Tutte le voci T14/T15/T18/T19/T20 sono state implementate nella sessione 2026-07-18 dopo una ricerca web/GitHub dedicata per ognuna (fonti primarie: PE/COFF Microsoft docs, CommonLibSSE-NG, xEdit changelog ufficiale, repo BethINI Pie, NGIO/GrassPrecacher) — vedi dettaglio per voce sotto. +47 test (99 file/1043 totali, tutti verdi), `tsc`/`vite build`/lint puliti.
- ~~**T14** — Preflight DLL SKSE~~ → ✅ FATTO: `electron/launch/skseDllPreflight.ts` (parser PE puro: DOS→PE→Optional Header/DataDirectory→Section Table→Export Directory→risoluzione per nome, nessuna libreria; legge la struct `SKSEPlugin_Version`/`PluginVersionData` 0x350 byte e confronta `compatibleVersions[]`/flag version-independent col runtime). **Validato contro 5 DLL SKSE reali installate** (nomi/versioni/AddressLibrary estratti correttamente), non solo fixture sintetiche. IPC `skse:preflight-dlls` + card "Preflight DLL SKSE" in Strumenti.
- ~~**T15** — Validazione header ESP~~ → ✅ FATTO: `electron/plugins/espValidate.ts` (range object-index ESL: 0x800-0xFFF sempre valido, 0x001-0x7FF richiede HEDR.version≥1.71; report form43/44 informativo, mai bloccante — esteso `eslify.ts`'s `scanPluginRecords` con `formVersionCounts`/`ownRecordObjectIndices`). **Validato contro 4/5 .esl reali** (il 5° era un errore di path, non del codice). IPC `plugins:validate-esp` + card "Valida plugin ESP" in Strumenti.
- ~~**T16** — "Save Doctor"~~ → ✅ FATTO (2026-07-16, invariato): `electron/saves/` (lz4.ts decoder block puro con prefix-decode, essParser.ts fail-soft SE/AE con doppio layout formVersion±gameVersion string, saveDoctor.ts diff vs plugins.txt di sistema ∪ file Data). IPC `saves:doctor` + warning preflight `VerifyLoadOrder` ("Ultimo salvataggio a rischio", mai bloccante, mai spurio: checked:false su ogni ambiguità).
- ~~**T17** — Rilevamento external-changes sul deploy~~ → ✅ FATTO (2026-07-16, invariato): `electron/deploy/verifyDeploy.ts` (manifest vs disco: file assente=missing, nlink=1=sostituito esternamente, junction scollegate). IPC `deploy:verify` + bottone "Verifica" nel DeployPanel + check preflight `VerifyManifest`.
- **NUOVO (2026-07-16)** — Protezione aggiornamenti Steam (da ricerca web round 3): `electron/steam/updateGuard.ts` (appmanifest_489830.acf read-only) + drift versione runtime. IPC `updateGuard:status`/`set` + card in Impostazioni.
- ~~**T18** — INI tuner da BethINI Pie~~ → ✅ FATTO: `electron/ini/bethiniPresets.ts` — NON un import del dataset completo (repo `DoubleYouC/Bethini-Pie-*`, `settings.json` CC BY-NC-SA 4.0 non redistribuibile), ma una tabella curata con i valori esatti per-tier verificati da fonte primaria per Grass/Distant Detail/Shadow (8 chiavi ini, flavor "bethini" ottimizzato + "vanilla" ufficiale). Applicato via l'editor line-oriented esistente (`iniService.applyIniSettings`, mai un parse→reserialize distruttivo). IPC `ini:apply-bethini-preset` + card in Strumenti.
- ~~**T19** — Grass cache autopilota~~ → ✅ FATTO (con limite onesto): generare il CONTENUTO della cache richiede **inevitabilmente** il gioco reale in esecuzione (25min-2,5h, crash attesi — nessun tool headless esiste, verificato su NGIO/GitHub/Nexus/STEP/Nolvus). Implementato ciò che è realmente automatizzabile: `electron/launch/grassCache.ts` (parsing `.cgid`, prerequisiti ini) + `grassCacheEngine.ts` (`supervisePrecache`: lancia il gioco, rilancia sui crash finché NGIO non rimuove il marker `PrecacheGrass.txt`, stesso pattern di Al12rs/modorganizer-GrassPrecacher). IPC `grass:status`/`start-precache`/`clear-marker` + card "Grass Cache — Autopilota" in Strumenti (copy onesta su durata/crash attesi).
- ~~**T20** — Auto-clean QAC via SSEEdit~~ → ✅ FATTO (nota superata: il provisioning GitHub esistente in `tools/provision.ts` scarica già xEdit — "tool non installato" non è più un blocco): `electron/plugins/qacRunner.ts` (flag CLI esatti `-QAC -autoload -autoexit -D: -P: -<game>`, guardia anti-master-ufficiale) + `qacEngine.ts` (spawn con watchdog timeout — nessun exit-code contrattuale documentato per xEdit — classificazione dal log `*Edit_log.txt`/`*EditException.log`, pattern PACT). IPC `plugins:qac-clean` + bottone "Pulisci" per plugin dirty nel DeployPanel.

## 🚢 Deployment

> ## ⛔ DECISIONE DI SCOPO (2026-07-17, dall'utente): IL LAUNCHER NON VA DISTRIBUITO A NESSUNO.
> Progetto a **utente singolo** (l'utente è anche chi lo costruisce). Conseguenze — da NON riproporre come lavoro aperto:
> - **GitHub Release / auto-update: NON SERVE.** Alimenterebbe `electron-updater` per installazioni su *altri PC*. Qui ogni `npm run electron:build` riscrive `release/win-unpacked`, a cui punta il collegamento sul Desktop: l'utente ha già la versione nuova nell'istante in cui la build finisce. Senza feed non si rompe niente: `checkForLauncherUpdate` è best-effort per costruzione (`electron/launch/launcherUpdate.ts` — "A missing/unreachable feed must NEVER block launch": warning nel log, avvio prosegue).
> - **Firma dell'installer / SmartScreen (SRB-003): NON SERVE.** Stessa logica: l'avviso SmartScreen protegge chi scarica software da terzi. L'utente esegue una build che ha appena prodotto sulla propria macchina.
> - Il codice per entrambe **resta pronto e testato**: se un giorno lo scopo cambia, basta pubblicare una Release (o fornire `CSC_LINK`) e le funzioni si accendono senza modifiche.
> - Sessioni precedenti le elencavano tra i "bloccanti": era un errore di framing — spuntavano una casella, non risolvevano un problema dell'utente. Le voci sotto sono conservate come **storia tecnica**, non come lavoro da fare.

### ~~D1 — Produrre l'installer NSIS~~ → ✅ FATTO e RIVERIFICATO (build fresco 1.0.1 del 2026-07-16, PIVOT-13 CHIUSO)
- **Build 1.0.1 (2026-07-16, stack corrente):** `release/Skyrim-AE-Fantasy-Launcher-Setup-1.0.1.exe` (132 MB) + `latest.yml` (version 1.0.1, url=file, sha512, size coerenti) + `.blockmap`, prodotti con Electron 43.1.0 / electron-builder 26 / Vite 8 / Node 24 e le opzioni reali `disableAsarIntegrity:true` + `npmRebuild:false` (modulo nativo FOMOD: prebuilds shippate, npmRebuild le romperebbe). App impacchettata (win-unpacked rigenerata) avviata e verificata VIVA: 4 processi, provider Nexus attivo, log boot pulito. Il collegamento Desktop esistente resta valido (punta a win-unpacked). Include le feature GUARD-01 (protezione update Steam, verifica deploy, Save Doctor). ~~Unico residuo per auto-update: pubblicare la GitHub Release~~ → **superato dalla decisione di scopo sopra: nessuna distribuzione, nessuna Release necessaria.** (Build corrente: **1.0.2**, include anche TOOLS-01 e T11.)
- **Installer prodotto:** `release/Skyrim-AE-Fantasy-Launcher-Setup-1.0.0.exe` (89.7 MB, NSIS, ~~electron 29.4.6~~ — stack aggiornato a Electron 43.1.0/electron-builder 26, vedi nota sotto, better-sqlite3 ricompilato ABI electron) + `latest.yml` (feed auto-update, url=file coerente, niente spazi) + `.blockmap` (delta update). `oneClick:false`, `perMachine:false` (no admin, dati preservati), shortcut Desktop+StartMenu "Skyrim AE Fantasy Launcher" con dragon icon, `runAfterFinish`, `deleteAppDataOnUninstall:false`. winCodeSign in cache → nessun problema symlink. **Non firmato** (`signAndEditExecutable:false`) → SmartScreen segnalerà l'app al primo avvio (normale per installer non firmati).
- ~~**Residuo auto-update**~~ → **NON APPLICABILE** (decisione di scopo 2026-07-17: nessuna distribuzione). Conservato come istruzione dormiente: *se* un giorno si distribuisse, basterebbe pubblicare una GitHub Release su `guidonossardi001-hue/skyrim-mod-manager` allegando i 3 file (installer + `latest.yml` + `.blockmap`); i bump di `version` + nuova release aggiornerebbero i client via `electron-updater`. Firma: `CSC_LINK`/`CSC_KEY_PASSWORD`.
- **Nota (2026-07-16, PIVOT-13):** dopo questa build sono stati aggiunti `build.disableAsarIntegrity:true` (necessario per Smart App Control con lo stack attuale) e `build.npmRebuild:false` (necessario per il modulo nativo FOMOD, PIVOT-08). Questo installer 1.0.0 **non riflette** questi due cambi né lo stack reale attuale (Electron 43.1.0, better-sqlite3 12.11.1, electron-builder 26, Vite 8, Node 24) — lo stato "GO_NO_GO packaging già verificato" va considerato **da riverificare** con un build NSIS fresco su questo stack, non dato per assodato. Non risulta (in questa sessione) che un build fresco con queste opzioni sia già stato prodotto: onestamente da verificare, non affermare né negare.

### D1-storico — dettagli tecnici (pre-2026-07-10)
- **Contesto:** l'app è già impacchettata (`release/win-unpacked/`, better-sqlite3 unpacked); manca solo il wrapper NSIS. `electron-builder` NSIS fallisce in questo ambiente perché winCodeSign richiede la creazione di symlink (privilegio negato).
- **Fatto (2026-07-09):** l'icona placeholder che avrebbe fatto fallire il build ora esiste (vedi PROD-01) — questo blocco è rimosso dal percorso critico.
- **Fatto (2026-07-10, LAUNCH-01):** config NSIS definitiva — icona dragon (`assets/dragon_launcher.ico`) sostituisce il placeholder su installer/uninstaller/header, shortcut Desktop+StartMenu auto-creati col nome "Skyrim AE Fantasy Launcher", `perMachine:false`+`deleteAppDataOnUninstall:false`, `publish` GitHub per `electron-updater`. `package.json` dep aggiunta: `electron-updater`.
- **Azione:** prima di buildare, allineare `node_modules` alla config (`package.json` già pronto: Electron `^33.0.0`, `better-sqlite3` `^11.3.0`) con `npm install` (rebuild ABI nativo). Poi sulla macchina di build abilitare **Windows Developer Mode** (Impostazioni → Privacy e sicurezza → Per sviluppatori) **o** eseguire il terminale come **Amministratore**, poi `npm run electron:build` (= `build` + `electron-builder`, target `nsis`). Per release firmata: fornire certificato (`CSC_LINK`/`CSC_KEY_PASSWORD`); oggi `signAndEditExecutable: false` → installer non firmato (SmartScreen segnalerà l'app).
- **Verificato:** `--dir` produce l'app spacchettata correttamente; smoke runtime PASS. Boot reale via `avvia_launcher.bat` confermato il 2026-07-09. Config NSIS/icona/publish non ancora esercitata con un build reale (node_modules ferma su Electron 29/better-sqlite3 9 in questo ambiente).
- **AGGIORNATO A (2026-07-16):** questi riferimenti a Electron `^33.0.0`/`^11.3.0` e "node_modules ferma su Electron 29/better-sqlite3 9" sono storici e superati — lo stack reale oggi è Electron 43.1.0, better-sqlite3 12.11.1, electron-builder 26, Vite 8 (rolldown, no esbuild), Vitest 4, Node 24 (vedi sezione "Pivot architetturale 2026-07-16"). `npm install` non è più solo "azione utente residua": il progetto gira su questo stack più recente, con in più `npmRebuild:false` richiesto dal modulo nativo FOMOD (PIVOT-08/13) — un build NSIS fresco su questo stack esatto resta da riverificare (vedi D1 sopra).

## Note QA
Prima di chiudere ogni sessione: `npm test` verde, `npm run lint` (0 errori), `npm run typecheck` (0 errori), `npm run build` OK, aggiornare gli 8 documenti di stato. Da CI (GitHub Actions, `windows-latest`): stessa sequenza lint→typecheck→test ad ogni push/PR — vedi `.github/workflows/ci.yml`.
- **Stato test corrente (2026-07-16):** ~749 test / 76 file, tutti verdi; `tsc --noEmit` pulito; `vite build` OK — sostituisce i conteggi più vecchi citati altrove nel file (95/220/266/447/etc), mantenuti solo come traccia storica di progressione.
